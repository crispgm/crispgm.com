# coding: utf-8
task default: %w[serve]

task :init do
  sh "bundle install"
end

task :serve do
  sh "bundle exec jekyll serve"
end

task :build do
  sh "rm -rf ./_site"
  sh "bundle exec jekyll build --destination=../site"
  sh "rm -rf ./_site"
end
